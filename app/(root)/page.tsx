import { auth } from "@/auth";

import React from "react";

const Home = async () => {
  // const session = await auth();
  // console.log(session);
  return (
    <>
      <h1 className="h1-bold">Siehh</h1>
    </>
  );
};

export default Home;
